/**
 * @author Kate Compton
 */

define(["./nightvale", "./interruption", "./custom"], function(nightvale, interruption, custom) {'use strict';
    return {
        nightvale : nightvale,
        interruption : interruption,
        custom : custom,
    };
});
